<%= form_with(model: helloworld, local: true) do |form| %>
  <% if helloworld.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(helloworld.errors.count, "error") %> prohibited this helloworld from being saved:</h2>

      <ul>
      <% helloworld.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :helloworld_name %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :helloworld_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
